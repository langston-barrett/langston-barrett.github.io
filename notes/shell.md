# Shell

## `echo`

### `-n`

<!-- marki[card] -->

Q. What does the `-n` flag of `echo` do?

A. Does not print a trailing newline.

<!-- marki[card] -->

Q. What `echo` flag causes it to not print a trailing newline?

A. `-n`
